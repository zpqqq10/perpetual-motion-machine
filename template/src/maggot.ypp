%{
#include <stdio.h>
#include <string.h>
#include <iostream>
#include <string>
#include "main.h"

using namespace std;
extern int yylex(void);

extern int yyparse();
int yyerror(const char *s);

%}

%token NUMBER


%%


lines
	: exp {printf("test\n");}
	;
exp 
	: NUMBER
	;


%%


int yyerror(const char *s)
{
	error("%s", s);
	return 0;
}

